# Pinned upstream commits.
#
# Read by scripts/fetch-spec.sh at build time. The running server
# never reads this file — it reads the baked `build/*.json`
# artifacts that this pin produced.
#
# Format: one `key=sha` per line. `#` starts a comment.

spec/main=2e41952fd64b13738d26b9b43e2a30bed914671a
proposals/main=0fdc795b660d84217ca0b10ee7adceac1bb5be4a
